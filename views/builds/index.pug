html
    head 
        title=title
    body
        header
            h1 Character Builds
            nav 
                ul
                    li 
                        a(href='/') Main Hub
                    li 
                        a(href='/builds/new') Create a New Build
        main
            h2 My Characters
            ul
                for build in builds
                    li=build.name
                        a(href=`/builds/${build._id}`) build.id

                             