link(rel="stylesheet", href="/css/style.css")

.nav
    .nav-header 
        .nav-left
            a(href="/" .nav-link) 
                img(src="https://i.redd.it/gwsphxkkwyl31.png", alt="cyberpunk2077 logo" width=300 height=auto)
        .nav-center
        .nav-right
            a(href="/registration" .nav-link) Registration
            a(href="/login" .nav-link) Login
            .hamburger-menu
                .bar
                .bar
                .bar
    .overlay    
        .overlay-content
            a(href="#" .overlay-link) Official Website
            a(href="https://twitter.com/CyberpunkGame" .overlay-link) Twitter 
            a(href="#" .overlay-link) Youtube

script.
    const hamburgerMenu = document.querySelector('.hamburger-menu');
    const overlay = document.querySelector('.overlay');
    hamburgerMenu.addEventListener('click', function() {
        hamburgerMenu.classList.toggle('open');
        overlay.classList.toggle('show');
    })

